function show(id){document.querySelectorAll('main>section').forEach(x=>x.hidden=true);document.getElementById(id).hidden=false;scrollTo(0,0)}document.getElementById('doc').addEventListener('change',e=>{document.getElementById('docname').textContent=e.target.files[0]?'Выбран: '+e.target.files[0].name:'Файл ещё не выбран'});function demoImport(){const f=document.getElementById('dirfile').files[0];document.getElementById('importstatus').textContent=f?'Файл выбран: '+f.name+'. Следующий этап — разбор колонок и сопоставление полей.':'Сначала выберите файл.'}